---
# Ansible-etcd for CoreOS
# include these variables on CoreOS hosts

etcd_config_only: true
etcd_daemon: "systemctl"

etcd_user: etcd
etcd_interface: "eth1"
etcd_dir: "/usr/bin"
etcd_cmd: "{{ etcd_dir }}/etcd2"
etcd_data_dir: "/var/lib/etcd2"
